fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
requests[socks]>=2.31.0
urllib3>=2.0.0
beautifulsoup4>=4.12.0
selenium>=4.18.0
webdriver-manager>=4.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
aiofiles>=23.2.0
slowapi
langdetect
playwright
trafilatura>=1.6.0
py3langid>=0.2.0
starlette>=0.36.0
python-multipart>=0.0.9
passlib[bcrypt]
python-jose[cryptography]
lxml>=5.0.0

# ── Adaptive discovery layer (backend/discovery/) ────────────────────────────
# crawl4ai powers the preferred sitemap/deep-crawl paths (AsyncUrlSeeder with
# BM25 relevance scoring, BestFirstCrawlingStrategy). It is OPTIONAL: every
# strategy in backend/discovery/ ships a pure requests+lxml fallback and the
# package is imported behind a CRAWL4AI_AVAILABLE guard, so the app runs without
# it — just with weaker discovery. Installing it also needs a browser:
#     pip install crawl4ai && playwright install chromium
# NOTE: as of this commit the crawl4ai code paths have never been executed. All
# 74 tests in backend/discovery/ exercise the fallbacks. Expect to debug the
# crawl4ai paths on first real run. See backend/discovery/README.md.
crawl4ai>=0.9.2

# Optional — only needed for XLSX/Parquet export
# pandas>=2.2.0
# pyarrow>=15.0.0
